<% if @profile.user == current_user %>

  <%= form_for [current_user, @profile], html: { id: 'submitBiography' }, remote: true do |f| %>
    <%= f.text_area :biography %><br>
    <%= f.submit %>
  <% end %>

<% end %>
