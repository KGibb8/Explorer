<% if @profile.user == current_user %>

  <%= form_for [current_user, @profile], html: { id: 'submitAvatar' }, remote: true do |f| %>
    <%= f.file_field :avatar %><br>
    <%= f.submit "Change Avatar" %>
  <% end %>

<% end %>

